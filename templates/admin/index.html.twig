{% extends 'base.html.twig' %}

{% block body %}

<div class="container mt-4">
<h1>Gérer les biens</h1>
    <table class="table-striped">
<thead><tr><th></th></tr></thead><tbody>{% for property in properties %}<tr><td>{{property.title}}</td><td><a class="btn-btn-secondary" href="{{path('admin.property.edit', {d:property.id})}}">Editer</a></td></tr>{% endfor %}</tbody></table>
</div>
    </div></div>
{% endblock %}
